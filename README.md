## Minimal NextJS project template

ReactJs project with [NextJS](https://nextjs.org/) and linting configured.

## Prettier

Using default prettier configuration

## ESLint

Using Airbnb

## Setup

1. `yarn install`
2. `npm run dev`
3. Visit `http://localhost:3000`
